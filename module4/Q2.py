#program to create function calculation() such that it can accept two variables and calculate addition and subtraction and it must return both in a single return call.
def calculate(a,b):
    add = a+b
    sub = a-b
    return add,sub
print(calculate(40,10))
    
